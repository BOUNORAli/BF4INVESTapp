# Configuration Nixpacks pour Railway
# Railway utilisera Nixpacks pour builder et déployer

[phases.setup]
nixPkgs = ["maven", "jdk17"]

[phases.setup]
nixPkgs = ["maven", "jdk17"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[start]
# Utiliser le script start.sh qui trouve automatiquement le JAR
cmd = "bash start.sh"

